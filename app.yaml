runtime: python
env: standard
entrypoint: gunicorn -b :$PORT core.wsgi:application

handlers:
- url: /static              
  static_dir: staticfiles/  
- url: /media               
  static_dir: mediafiles/   
- url: /.*
  script: auto

runtime_config:
   runtime_version: "3.8"